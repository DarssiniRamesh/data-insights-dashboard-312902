test("dashboard_management: dashboard shows tracked data assets and supports remove (TODO)", () => {
  // TODO(dashboard_management):
  // - Render DashboardPage
  // - Seed localStorage with tracked data asset IDs (via submissionStore)
  // - Assert list renders
  // - Click remove and assert it disappears + localStorage updated
  expect(true).toBe(true);
});
