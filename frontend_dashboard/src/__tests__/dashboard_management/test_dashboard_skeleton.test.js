test("dashboard_management: dashboard shows tracked submissions and supports remove (TODO)", () => {
  // TODO(dashboard_management):
  // - Render DashboardPage
  // - Seed localStorage with tracked submission IDs (via submissionStore)
  // - Assert list renders
  // - Click remove and assert it disappears + localStorage updated
  expect(true).toBe(true);
});
